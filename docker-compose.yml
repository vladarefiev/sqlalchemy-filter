version: '3'
services:
  postgres:
    image: "postgres"
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=pylter
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=pylter
    volumes:
      - postgres-data:/var/lib/postgresql/data/
volumes:
  postgres-data: